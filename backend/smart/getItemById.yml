info:
  name: getItemById
  type: http

http:
  method: GET
  url: http://localhost:5000/api/items/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
