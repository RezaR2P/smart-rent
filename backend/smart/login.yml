info:
  name: login
  type: http

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "budi1@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
